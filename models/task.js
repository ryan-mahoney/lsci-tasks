module.exports = (aws, commit) => {
  return {
    run: (task) => {
      // start timer

      // run a task synchronously

      // capture output

      // convert ANSI to HTML

      // save output to S3

      // end timer

      // call home with result

      // run subtasks (recursive?)

      // return promise when all subtasks are done
    }
  };
};
